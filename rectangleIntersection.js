var test = require("tape");

function rectIntersec(a,b) {
  max(a[0], b[0]) - min(a[1], b[1]);
}
test("expects 2 rectangles and retunrs the intersected rectangle if any", function(t){
  t.test("when passed ")
})
